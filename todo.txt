what kind of music by tom misch and use of dayz
arlo parks eugene
loyle carner mean it in the morning
hunnybee unkown mortal orchestra
arlo parks cola


console.dir(element)
element.clientWidth = width without border
element.offsetWidth = width including borders
element.offsetLeft = the page X


Search by enter and take to a search page if there are more than 1 results in search dropdown
Show latest 5 callers but navigate to a page which shows callers from past 24 hours.













<div className={styles.task_tabs}>
        <div className={`active_bar ${styles.task_tabs__active_bar}`}></div>
        {taskTypes.map((taskType) => {
          return (
            <button
              className={`${styles.task_tab} ${
                taskType === taskTab.activeTask
                  ? `${styles.task_tab__active} active`
                  : ""
              }`}
              key={taskType}
              onClick={(e) => setActive(e, taskType)}
            >
              {taskType}
            </button>
          );
        })}
      </div>